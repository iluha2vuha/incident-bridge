# Claude Design Handoff Notes

Source archive: `source-export.zip`

Extracted prototype files are in `prototype/`.

Standalone prototype: `prototype.html`, copied from `prototype/Incident Bridge - Prototype.dc.html`.

The provided archive did not include `reference.pdf`; add it here if it is exported or supplied later.

Implementation note: the `.dc.html` files use a generated design-runtime format and should be treated as visual and interaction reference, not production React/TypeScript source.
